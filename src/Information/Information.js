import Axios from "axios";
import React, { useEffect, useState } from "react";
import * as S from "./styles";

const Information = ({ match, matchIn }) => {
  console.log(match);
  console.log(match.params);
  return <S.MainDiv>{}</S.MainDiv>;
};

export default Information;
